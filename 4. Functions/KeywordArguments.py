def increment(number, by):
    return number + by


print(increment(number=2, by=1))
# in the above line number=2 and by=1 are keyword arguments which tell what value
# is being passed just to make the code more readable
