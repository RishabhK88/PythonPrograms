# Below are lists
letters = ["a", "b", "c"]
# We can have lists of lists
matrix = [[0, 1], [2, 3]]
# We can make lists in the following manner
zeroes = [0] * 5
# We can operate on lists
print(zeroes)
combined = zeroes + letters
print(combined)
# We can iterate lists
print(list(range(20)))
chars = list("Hello World")
print(chars)
print(len(chars))
print(len(letters))
